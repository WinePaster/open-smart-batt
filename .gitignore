# ── 淨室紅線（clean-room）—— 這一段優先於底下所有分類 ──────────────────
# 逆向工程工作資料與原廠產物：**永不進 git、永不散佈**（見 CLEANROOM.md）。
#
# 為什麼寫在最上面、而且不厭其煩：
#   本專案已經記過一次同型破口 —— `raw/` 長期不在忽略清單裡，而文件一直宣稱它在
#   （2026-07-30 於 pro 修補）。目錄當時是空的所以無實害，但那是一個真實的缺口：
#   有人放檔進去，一次 `git add -A` 就收走了。
#   2026-08-24 實查本檔，發現公開 repo 這邊**同樣的三條從來沒有寫進來過**
#   （當時只有 `*.apk`），補上時 `git ls-files` 對這三類的追蹤數是 0 ⇒ 補上零風險。
#
# ⚠️ 這與 `.github/workflows/leak-gate.yml` 是**兩層不同的防線，缺一不可**：
#   本檔擋的是「東西根本進不了索引」；leak-gate 擋的是「已經 commit 進來才被抓到」。
#   ⇒ 兩者都綠，才是兩道門都關著。
# ⛔ 移除或收窄這一段的任何一行，需要擁有者裁示。
research/
raw/
*.zip

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/

# Flutter / Android (future app)
**/.dart_tool/
**/build/
*.apk
*.aab
.gradle/
local.properties
key.properties
*.keystore
*.jks
*.iml
.idea/

# Compiled / native / decompiled binaries — NEVER commit (vendor IP risk)
*.so
*.dex
*.aar
*.class
*.smali

# Captures / private data — NEVER commit raw logs or DB (may contain personal data)
btsnoop*
*.btsnoop
bugreport*
*.ab
*.db
*.db-wal
*.db-shm
phone_db/

# OS / editor
.DS_Store
*.swp
.vscode/

# Agent scratch. `.claude/worktrees/` holds full checkouts of this repo —
# hundreds of MB of working trees and build output that must never be staged.
# It was untracked but NOT ignored, which is one `git add -A` away from a mess;
# the same gap that let `raw/` sit outside this file for weeks (see CLAUDE.md).
.claude/
