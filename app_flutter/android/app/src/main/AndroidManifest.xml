<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- BLE permissions (OpenSmartBatt).
         Android 12+ (API 31+): runtime BLUETOOTH_SCAN + BLUETOOTH_CONNECT.
         Pre-12: legacy BLUETOOTH / BLUETOOTH_ADMIN + ACCESS_FINE_LOCATION
         (location is required to scan for BLE devices below API 31). -->
    <!-- check GitHub for app updates -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Foreground service that keeps the process alive while a pack is
         connected, so the screen going off does not freeze the BLE loop
         (design 0008). connectedDevice is the correct type for BLE telemetry
         AND it is the one with no time limit: Android's 6-hours-per-24 cap
         applies only to dataSync and mediaProcessing, so using either of those
         would cut a long ride or an overnight parked watch short.
         Starting the type requires FOREGROUND_SERVICE_CONNECTED_DEVICE plus one
         of BLUETOOTH_CONNECT/SCAN (declared above); missing either throws
         SecurityException at startForeground(). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Android 13+: the ongoing notification. NOT a prerequisite for the
         service; if the user denies it the watch still runs and only the
         notification is hidden (design 0008 §3.4). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- BLE is required hardware for this app. -->
    <uses-feature android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:label="OpenSmartBatt"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Keeps the process at foreground importance while connected. Does
             not touch Bluetooth itself; see MonitorService.kt.

             stopWithTask: the service must not outlive the engine it serves.
             Swiping the app away destroys the Activity and with it the
             FlutterEngine, and flutter_blue_plus disconnects every device from
             onDetachedFromEngine — so the link is already gone. Without this
             attribute the foreground service survived, holding the process up
             to display an ongoing "monitoring" notification frozen at the last
             reading, with nothing behind it (design 0038 §1.2).

             ⚠️ If design 0039 adopts a cached engine (engine outlives the
             Activity, so the link survives a task swipe), this attribute MUST
             be removed. Leaving it would kill the foreground service while the
             engine keeps running: the process drops back to cached importance
             and is frozen silently — exactly what design 0008 exists to
             prevent, and nothing would report it. -->
        <service
            android:name=".MonitorService"
            android:exported="false"
            android:stopWithTask="true"
            android:foregroundServiceType="connectedDevice" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: open external https links (GitHub / docs) on Android 11+ -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
