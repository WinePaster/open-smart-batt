/// OpenSmartBatt — platform-channel layer barrel.
///
/// Everything that talks to native code lives behind these handles, so the
/// state layer stays testable with plain fakes.
library;

export 'alert_notifier.dart';
export 'monitor_service.dart';
