name: open_smart_batt
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
#
# 版號規則見 docs/VERSIONING.md。兩件事必須知道：
#   * x.y.z 是最近一次「已發布」的版號，由 release 流程維護，請勿手改後不發版。
#   * `+0` 是刻意的。真正的 build number 一律由建置時的
#     `--build-number="$(tool/build_number.sh)"` 帶入；`+0` 只是「沒帶旗標」
#     時的後備值，會產生 versionCode 0 的 APK —— 它裝不到任何已發布版本之上，
#     等於明確標示「這是開發建置，不可散佈」。
version: 0.7.12+0

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # BLE transport
  flutter_blue_plus: ^1.32.0
  # Local storage (history, saved devices, settings, diagnostic log)
  sqflite: ^2.3.0
  path: ^1.9.0
  path_provider: ^2.1.0
  # State management
  provider: ^6.1.0
  # Runtime permissions (BLE / location / background)
  permission_handler: ^11.0.0
  # i18n / formatting
  intl: ^0.20.0
  # CSV / .log export + share sheet
  share_plus: ^10.0.0
  csv: ^6.0.0
  # open external links (GitHub / docs)
  url_launcher: ^6.3.0
  # keep the screen awake while connected (monitoring while riding)
  wakelock_plus: ^1.2.0
  # read the real APK version for the About screen (single source of truth)
  package_info_plus: ^8.0.0
  # GitHub release update check
  http: ^1.2.0
  # GNSS speed for the riding watchface (design 0042). Chosen over `location`
  # for `LocationAccuracy.bestForNavigation` and for exposing speedAccuracy
  # alongside speed; permissions stay on the app's existing permission_handler
  # flow rather than the plugin's, so there is one location grant, not two.
  # Foreground only — no background modes are declared (0042 N1).
  geolocator: ^14.0.2
  # Accelerometer for the G meter (design 0045). Two streams are used:
  # `accelerometerEvent` (gravity included) only inside the calibration window
  # and the still-window validity check, and `userAccelerometerEvent` (the OS's
  # gravity-free fusion, §3.3 case 甲) while riding.
  # No permission on Android below 200 Hz — this samples at about 50 (§2.2 #3).
  # The rotation and smoothing are pure Dart in `state/g_force_estimator.dart`,
  # which imports nothing from here, so machine-to-machine differences in sensor
  # quality change the INPUT and never the logic (R4).
  sensors_plus: ^7.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # On-device e2e harness (integration_test/). Boots the app on a real device
  # to smoke-test the first frame; the BLE paths still need a physical iPhone +
  # RCE battery (see integration_test/app_test.dart).
  integration_test:
    sdk: flutter

  # In-memory / headless sqflite for unit-testing the data layer.
  sqflite_common_ffi: ^2.3.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates Android launcher + adaptive icons from assets/icon/*.
  flutter_launcher_icons: ^0.14.1

# Launcher icon generation (run: dart run flutter_launcher_icons)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
  # iOS App Store rejects icons with an alpha channel; use a pre-flattened
  # opaque 1024x1024 copy (icon.png onto #0B0D11) for the iOS icon set only.
  image_path_ios: "assets/icon/icon_ios_opaque.png"
  adaptive_icon_background: "#0B0D11"
  adaptive_icon_foreground: "assets/icon/icon_fg.png"
  min_sdk_android: 21

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Enables gen_l10n localization codegen on build / pub get.
  generate: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
